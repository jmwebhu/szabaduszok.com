{% import 'User/write.twig' as buttons %}

<div class="panel">
	<div class="panel-body">
		<p>Itt megadhatod más oldalakon szereplő profiljaid URL -jét, amit a felhasználók meg tudnak tekinteni.</p>

		{% for profile in profiles %}

			<div class="form-group">
				{% if profile.icon_type_group == 'span' %}
					<span class="{{ profile.icon_type }} {{ profile.icon }} pull-left icon-sm icon-dark-gray"></span>									
				{% else %}
				{% endif %}
				<label>
					{{ profile.name }}
				</label>								

				<input type="text" name="profiles[]" id="{{ profile.profile_id }}" value="{% if in_array(profile.profile_id, userProfileUrls|keys) %}{{ userProfileUrls[profile.profile_id] }}{% endif %}" class="form-control" placeholder="{{ profile.name }}">																																							
			</div>
		{% endfor %}
                {{ buttons.nextButton() }} 
                {{ buttons.submitButton(needSteps) }}
                {{ buttons.prevButton() }}
	</div>
</div>
